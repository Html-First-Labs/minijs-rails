<%#
---
title: Dialog (Modal)
description: Use tailwind classes with mini js to show a modal.
---
%>

<script>
  isModalOpen = false;
</script>

<button
  class="relative inline-flex items-center justify-center gap-2 rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm font-semibold leading-6 text-gray-800 shadow-sm hover:bg-gray-50"
  type="button"
  :click="isModalOpen = true;"
>
  Open Modal
</button>

<div id="modal-outer" class="relative z-10 opacity-0 transition-all duration-300" aria-labelledby="modal-title" aria-modal="true" :class="isModalOpen ? 'opacity-100 pointer-events-auto' : 'opacity-0 pointer-events-none'" :click="if (event.target.id == 'modal-container-outer') { isModalOpen = false }">
  <div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity"></div>
  <div id="modal-inner" class="fixed inset-0 z-10 w-screen bg-black bg-opacity-25 h-screen top-0 left-0 transition-all" :class="isModalOpen ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-4'">
    <div id="modal-container-outer" class="flex min-h-full items-end justify-center p-4 sm:items-center sm:p-0">
      <div class="relative">
        <button class="ring-offset-background absolute right-2 top-2 rounded-full p-1.5 opacity-70 transition-opacity hover:cursor-pointer hover:bg-gray-100 hover:opacity-100 active:bg-gray-200 disabled:pointer-events-none" :click="isModalOpen = false" aria-label="Close Modal">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="text-gray-900 w-5 h-5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
        <div id="modal-container-content" class="row-start-2 w-full rounded-lg bg-white p-8 shadow-lg ring-1 ring-gray-400 sm:max-w-xl sm:rounded-2xl sm:p-6">
          <h2 class="mb-2 text-lg font-semibold">Are you sure?</h2>
          <p class="mb-2 text-sm">Are you sure you want to do that. Seriously?</p>
          <div class="flex w-full justify-end space-x-2 pt-2">
            <button class="relative inline-flex items-center justify-center gap-2 rounded-lg border border-gray-200 bg-white px-4 py-2 text-sm font-semibold leading-6 text-gray-800 shadow-sm hover:bg-gray-50" :click="isModalOpen = false;">Cancel</button>
            <button class="relative inline-flex items-center justify-center gap-2 rounded-lg border border-gray-700 bg-gray-900 px-4 py-2 text-sm font-semibold leading-6 text-gray-50 shadow-sm hover:bg-gray-800" :click="isModalOpen = false;">Continue</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
