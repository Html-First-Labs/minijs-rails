<style>

/* Container for the diff output */
.diff ul {
    list-style-type: none;
    padding: 0;
    margin: 0;
    font-family: monospace;
    white-space: pre-wrap;
}

/* Lines that have been deleted */
.diff .del del {
    display: block;
    color: #a00;
    background-color: #ffdddd;
    text-decoration: none; /* Removes the default strikethrough */
}

/* Lines that have been added */
.diff .ins ins {
    display: block;
    color: #008000;
    background-color: #ddffdd;
    text-decoration: none; /* Removes the default underline */
}

/* Optionally, for unchanged lines, if you want to style them */
.diff .unchanged span {
    display: block;
    color: #000;
}
</style>

<div class="accordion rounded-md overflow-hidden border border-gray-200 my-4">
  <section class="active grid transition-all border-gray-200 border-b hover:bg-gray-50" >
    <a class="cursor-pointer font-bold p-4 flex justify-between">
      Compare

      <div class="flex gap-x-2">
        <button class="rounded px-2" >

        </button>
      </div>

      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-gray-800">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"></path>
      </svg>
    </a>
    <div class="overflow-hidden">
      <div class="p-4 pt-0">
        <div>
          <%= Utilities.diff_samples(@recipe.content,Recipe.first.content).html_safe %>
          
        </div>


      </div>
    </div>
  </section>
</div>